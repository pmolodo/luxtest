# Forces a hard exit from hython - get a crash on exit in Ubuntu if RenderMan plugin is loaded

import atexit
import os


def hard_exit_success():
    os._exit(0)


atexit.register(hard_exit_success)
